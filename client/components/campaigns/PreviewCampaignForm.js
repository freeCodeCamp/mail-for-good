import React from 'react';

const PreviewCampaignForm = props => {
  const { handleSubmit, lastPage, form } = props;
  console.log(form);
  return (
    <h1>Test</h1>
  );
};

export default PreviewCampaignForm;
